// examples/demo.ltl — runnable demo for ltl_rbtree

import std::io
use ltl_rbtree as lib

fn main() {
    io::println("ltl_rbtree demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}