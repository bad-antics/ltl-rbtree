// ltl-rbtree/main.ltl
//
// Red-black tree with iterator, range scan, and persistence.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-rbtree v0.1.0")
    io::println("Red-black tree with iterator, range scan, and persistence.")
    0
}
